services:
  node-service:
    image: node-service:latest
    container_name: node-service
    build: .
    command: npm run dev
    restart: unless-stopped
    volumes:
      - .:/usr/src/app
    ports:
      - "${PORT}:${PORT}"
    env_file: .env
    tty: true
